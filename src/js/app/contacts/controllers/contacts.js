modContacts = angular.module('modContacts');

modContacts.controller('ctrlContacts', [
    function() {
        let vm = this;

        _init();

		/**
         * Called when module is initialized
         * @private
         */
        function _init() {
            // Load all data here
        }
    }
]);